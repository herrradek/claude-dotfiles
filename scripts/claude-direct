#!/usr/bin/env bash
# Claude Code connected directly to Anthropic (your paid subscription)
# Usage: claude-direct [args...]
# Requires ANTHROPIC_API_KEY to be set in your environment or ~/.claude.json

claude "$@"
